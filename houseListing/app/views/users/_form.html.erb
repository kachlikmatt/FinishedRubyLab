<%= form_for @user do |f| %>

  <% if @username.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@username.errors.count, "error") %> prohibited
        this username from being saved:
      </h2>
      <ul>
        <% @username.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :username %><br>
    <%= f.text_field :username %>
  </p>

  <p>
    <%= f.label :password %><br>
    <%= f.text_field :username%>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>
